/// Public dspec schema facade.
///
/// Stable imports should target this module. The implementation is split into
/// Core, Claims, and Checks modules to keep the public contract compact.
module dspec.Schema
amends "schema/Checks.pkl"
