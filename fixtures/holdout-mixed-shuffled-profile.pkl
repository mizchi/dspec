import "../dspec/Schema.pkl" as d

profile: d.AppProfile = new {
  id = "holdout-mixed-shuffled-profile"
  modelPath = "fixtures/holdout-mixed-model.pkl"
  appRoot = "fixtures/holdout-mixed-shuffled-app"
  requiredScenarioCategories {
    "route"
    "contract-schema"
    "workflow"
  }
}
