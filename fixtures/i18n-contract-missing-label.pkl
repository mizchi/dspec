import "../dspec/Schema.pkl" as d

model: d.Model = new {
  id = "i18n-contract-missing-label"
  name = new d.LocalizedText {
    default = "i18n 欠落 fixture"
    labels {
      ["ja"] = "i18n 欠落 fixture"
    }
  }
  version = "0.1.0"
  primaryLocale = "ja"
  locales {
    "ja"
    "en"
  }

  i18n = new d.I18nContract {
    requiredLocales {
      "ja"
      "en"
    }
  }
}
